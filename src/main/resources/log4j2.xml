<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <Console name="ConsoleAppender" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %c{1} - %msg%n" />
        </Console>
        <File name="FileAppender"
              fileName="./logs/MarketMonarch-${date:yyyyMMdd}.log"
              immediateFlush="true" append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n" />
        </File>
    </Appenders>

    <Loggers>
        <!-- 🛑 PDFBox & FontBox komplett deaktivieren -->
        <Logger name="org.apache.pdfbox" level="off" additivity="false" />
        <Logger name="org.apache.fontbox" level="off" additivity="false" />

        <!-- 📦 Projektinterne Logger -->
        <Logger name="com.rickauer.marketmonarch" level="trace" additivity="false">
            <AppenderRef ref="ConsoleAppender" />
            <AppenderRef ref="FileAppender" />
        </Logger>

        <!-- 🧱 Root logger (für alle nicht explizit gesetzten) -->
        <Root level="trace">
            <AppenderRef ref="ConsoleAppender" />
            <AppenderRef ref="FileAppender" />
        </Root>
    </Loggers>
</Configuration>
